package com.pedestriamc.strings.command;

@SuppressWarnings("all")
class ChannelCommandTest {

    /*ServerMock mock;
    Strings strings;

    @BeforeEach
    void setUp() {
        mock = MockBukkit.mock();
        strings = MockBukkit.load(Strings.class);
    }

    @AfterEach
    void tearDown() {
        MockBukkit.unmock();
    }

    @Test
    void test() {
        PlayerMock player = mock.addPlayer();
        createChannel();
        player.performCommand("channel oogabooga");
        StringsProvider.get().getStringsUser(player);
        assert(StringsProvider.get().getStringsUser(player).getActiveChannel().getName().equals("oogabooga"));
    }

    void createChannel() {
        StringsProvider.get().getChannelLoader().registerChannel(new MockChannel("oogabooga"));

    }*/

}
